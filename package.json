{
  "name": "admin-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server --watch db.json --port 3001\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3001",
    "start": "concurrently \"npx serve -s dist -l 3000\" \"json-server --watch db.json --port 3001 --host 0.0.0.0\""
  },
  "dependencies": {
    "react-router-dom": "^7.13.0",
    "lucide-react": "^0.563.0",
    "recharts": "^3.7.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^6.2.0",
    "json-server": "^0.17.4",
    "concurrently": "^9.1.2",
    "serve": "^14.2.1"
  }
}
